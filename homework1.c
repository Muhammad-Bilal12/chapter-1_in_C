#include <stdio.h>

void main()
{

    //! Question : 1
    // * Write a program to calculate the area of square? (Sides are given)
    //* area formula [area = length * width]
    /*
        int length, width;
        printf("Enter the value of Length : ");
        scanf("%d", &length);

        printf("Enter the value of Width : ");
        scanf("%d", &width);

        printf("The area of the rectangle is : %d", length * width);
    */
    //* ********************* DONE ******************************** //

    //! Question : 2
    // * Write a program to calculate the area of circle? (radius are given)
    //* area of circle: formula [area = pi*r*r]

    /*    int radius;
        float pi = 3.14;
        printf("Enter the radius of Circle : ");
        scanf("%d", &radius);

        printf("the radius of the circle is : %.2f", pi * radius * radius);
    */
    //* ********************* DONE ******************************** //

    //! Write a program to calculate perimeter of rectangle.
    //* Take sides, a & b, from the user.
    //* parimeter of the rectangle = 2(l*W)

    /*
    int length, width;
    printf("Enter the value of Length : ");
    scanf("%d", &length);

    printf("Enter the value of Width : ");
    scanf("%d", &width);

    printf("The perimeter of the rectangle is : %d", 2 * (length * width));
    */
    //* ********************* DONE ******************************** //
    //! Take a number(n) from user & output its cube(n*n*n).

    int num;
    printf("Enter the number : ");
    scanf("%d", &num);
    printf(" The cube of your number is : %d", num * num * num);

    //* ********************* DONE ******************************** //
    // ! homework 1 completed
}